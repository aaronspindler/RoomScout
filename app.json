{
   "environments": {
     "test": {
       "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"],
       "scripts": {
         "test": ["python manage.py test --parallel", "python manage.py check --deploy"]
       }
     }
   }
 }